#!/usr/bin/env bash

alias ..="cd .."
alias ...="cd ../.."

alias h='cd ~'
alias c='clear'
alias ll='ls -lah'
alias now='date +"%T"'
alias snano='sudo nano'
alias ports='netstat -tulanp'
alias header='curl -I'
alias wget='wget -c --no-check-certificate'

alias artisan='php artisan'
alias root='sudo -i'

alias apt-get='sudo apt-get'

alias webreload='sudo service nginx reload && sudo service php5-fpm reload'
alias webrestart='sudo service nginx restart && sudo service php5-fpm restart'
